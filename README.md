# Python To-Do App (Dockerized)

This is a simple To-Do web application built using Flask and Docker.

## Features
- Add tasks
- Delete tasks
- Simple UI

## Technologies Used
- Python (Flask)
- Docker
- Docker Compose

## How to Run

```bash
docker compose up --build
